# List all Python package requirements this project has.
#
# more details: http://www.pip-installer.org/en/latest/requirements.html

### General useful bits
py-bcrypt
PyYAML
Mercurial
PIL >= 1.1.7

### Database drivers
psycopg2

### Django related
Django>=1.4,<1.5
django-pagination
South


### Pombola dependencies
sorl-thumbnail
django_date_extensions
django-ajax-selects

Markdown

### Use a fork of django-markitup so that we get correct truthiness
-e hg+https://bitbucket.org/evdb/django-markitup/@fe49ef12c614#egg=django-markitup

# Testing helpers 
httplib2
WebTest>=1.3.2
django-webtest
-e git://github.com/nathforge/django-mechanize.git#egg=django-mechanize
selenium
django-selenium

BeautifulSoup
BeautifulSoup4
unidecode
python-memcached

# django-mapit
-e git://github.com/mysociety/mapit.git@master#egg=django-mapit

# SayIt
# Specifying popit-django master here too, because the -e line in sayit's
# requirements.txt is ignored installing this way, so otherwise it wouldn't be.
# If/when sayit specifies the pypi version of popit-django, the second line
# here would not be necessary.
-e git://github.com/mysociety/sayit.git@master#egg=django-sayit
-e git://github.com/mysociety/popit-django@master#egg=popit-django

python-dateutil

coverage>=3

requests

## votematch
django-model-utils
